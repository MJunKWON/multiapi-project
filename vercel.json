{
  "framework": "nextjs",
  "buildCommand": "cd packages/frontend && npm install && rm -rf .next && npm run build",
  "outputDirectory": "packages/frontend/.next",
  "installCommand": "cd packages/frontend && npm install",
  "builds": [
    {
      "src": "packages/frontend/package.json",
      "use": "@vercel/next"
    }
  ]
}